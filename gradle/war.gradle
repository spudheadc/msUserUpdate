apply plugin: "war"

bootWar {
    mainClass = "chapman.wattle.id.au.msuserupdate.MsUserUpdateApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
